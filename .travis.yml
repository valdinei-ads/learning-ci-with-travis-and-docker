language: php
php:
  - 7.3
  - 8.0
dist: focal
# branches:
#   only:
#     - master

env:
  - DOCKER_COMPOSE_VERSION=1.4.2

services:
  - docker

before_script:
  - sudo rm -rf /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  # - docker-compose up -d
  # - docker-compose ps

  # - apt-get install docker-compose
  # - cp .env.example .env
  # - composer install
  # - php artisan key:generate
  # - php artisan migrate
script:
    - ls -a
#   - docker exec -t micro-videos-app composer install
#   - docker exec -t micro-videos-app cp .env.example .env
#   - docker exec -t micro-videos-app php /var/www/artisan key:generate
#   - docker exec -t micro-videos-app php /var/www/artisan migrate
#   - docker exec -t micro-videos-app php /var/www/vendor/bin/phpunit -c /var/www/phpunit.xml

# after_success:
#   - ./scripts/dotnet-publish.sh
#   - ./scripts/docker-publish.sh
# notifications:
#   email:
#     on_success: never
#     on_failure: always
